server:
    port: 8082
spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/jpademo?useSSL=false
        username: postgres
        password: postgres
    jpa:
        hibernate:
            ddl-auto: create-drop
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
